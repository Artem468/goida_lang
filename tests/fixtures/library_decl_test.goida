// Тест для парсинга объявления библиотеки — хранится вне `examples/`
библиотека "native" {
    функция внешняя() -> число { }
    переменная MAGIC : число;
}

// Пример не исполняется в общем раннере `examples/`.
