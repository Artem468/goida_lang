класс Тест {
    публичный х: число;
    публичный у: число;
    
    конструктор создать(а: число, б: число) {
        это.х = а;
        это.у = б;
    }
}

т = новый Тест(10, 20);
печать(т.х);
печать(т.у);
т.х = 100;
т.у = 200;
печать("После изменения:");
печать(т.х);
печать(т.у);
