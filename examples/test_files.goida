// ф = новый Файл("./examples/path.txt");

// печать(ф.записать());

класс Строитель {
    публичный клс: пустота;

    публичный конструктор создать(_клс) {
        это.клс = новый _клс();
    }
}

класс Пользователь {
    приватный имя: строка = "ыыы";

    публичный статичный функция привет() -> строка {
        вернуть "привет мир";
    }

    публичный функция мое_имя() -> строка {
        вернуть это.имя;
    }

}

п = новый Пользователь();
печать(п.привет());
печать(п.мое_имя());


печать(Пользователь.привет());
