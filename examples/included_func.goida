функция внешняя() {
    функция привет() {
        печать("привет");
    }

    привет();
    привет();
    привет();
    привет();
    привет();
}


внешняя();


класс Тест {
    публичный функция метод(это) {
        класс ыыы{
            публичный функция абоба(это) {
                печать("123");
            }
        }

        в = новый ыыы();
        в.абоба();
    }
}

а = новый Тест();
а.метод();
