// Тест встроенных классов (строка, число, список, словарь)
s = "";
пустая_строка = s.длина() == 0;
печать(пустая_строка);

п = список();
для (i = 0; i < 3; i = i + 1) {
    п.добавить(i);
}
печать(п.длина());

д = словарь("k", 1);
печать(д["k"]);
