функция факториал(n:число) -> число {
    если (n <= 1) {
        вернуть 1;
    } иначе {
        вернуть n * факториал(n - 1);
    }
}


печать(факториал(5));  // 120


функция максимум(a: число, b: число) -> число {
    если (a > b) {
        вернуть a;
    } иначе {
        вернуть b;
    }
}

