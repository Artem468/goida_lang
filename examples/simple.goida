подключить "functions";

функция приветствие(имя:текст): текст {
    вернуть "Привет, " + имя + "!";
}

и = 5;
а = "йцуйцу";
печать(и + а);

печать(
    приветствие(
        "123"
        )
);

печать(functions.факториал(5));
