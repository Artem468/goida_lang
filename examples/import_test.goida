подключить "math_module";

// Тестируем создание класса из импортированного модуля
точка = новый math_module.Точка(3, 4);
печать("Создали точку с координатами x=" + точка.х + ", y=" + точка.у);
а = точка.расстояние_до_центра();
печать("Расстояние до центра: " + а);

вектор = новый math_module.Вектор(5);
печать(вектор);
печать("Создали вектор с длиной: " + вектор.длина);
печать("факториал: " + вектор.факториал123(5));
