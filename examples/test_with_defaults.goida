класс Тест {
    публичный х: число = 0;
    публичный у: число = 0;
    
    конструктор создать(а: число, б: число) {
        это.х = а;
        это.у = б;
    }
}

т = новый Тест(10, 20);
печать(т.х);
печать(т.у);
