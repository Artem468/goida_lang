[package]
name = "goida-lsp"
version = "0.1.0"
edition = "2021"

[dependencies]
goida-model = { path = "../goida-model" }
goida-runtime = { path = "../goida-runtime" }
goida-syntax = { path = "../goida-syntax" }
tower-lsp = "0.20"
tokio = { version = "1.0", features = ["full"] }
